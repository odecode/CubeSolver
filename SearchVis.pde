class SearchVis{
  Solver solver;
  Solver.Tree svtree;
  SearchVis(Solver s){
    this.solver = s;
    circle(0,0,50);
    
  }
  
  class Point(){
    float x;
    float y;
  
  }
  
  void drawTree(Solver.Tree tree, boolean n){
    this.svtree = tree;
    while(depth
    // dist 500, width 50, middle (0,0)
    
    }
    
    
      //<>//
  }
}
